 node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Run Cypress Tests with Recording
        run: npx cypress run --record --key ${{ secrets.CYPRESS_RECORD_KEY }} --parallel

      - name: Upload Cypress Videos and Screenshots (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-artifacts
          path: |
            cypress/videos
            cypress/screenshots
          retention-days: 7name: Cypress Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Kode
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Run Cypress Tests with Recording
        run: npx cypress run --record --key ${{ secrets.CYPRESS_RECORD_KEY }} --parallel

      - name: Upload Cypress Videos and Screenshots (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-artifacts
          path: |
            cypress/videos
            cypress/screenshots
          retention-days: 7
